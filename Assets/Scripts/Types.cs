using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Types
{
    public enum TextType
    {
        FONT,
        COLOR
    }

    public enum BoxType
    {
        SIZE,
        COLOR
    }

    public enum AviType
    {
        COLOR,
        IMAGE
    }
}
